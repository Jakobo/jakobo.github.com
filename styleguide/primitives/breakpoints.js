const sm = 480
const md = 768
const lg = 1200

const breakpoints = {
  xs: `(max-width: ${sm - 1}px)`,
  sm: `(min-width: ${sm}px)`,
  md: `(min-width: ${md}px)`,
  lg: `(min-width: ${lg}px)`
};

export default breakpoints;
