import React, { PropTypes } from "react"
import {StyleRoot, Style} from 'radium';
import Homepage from "./homepage"

const Felocity = () => {
  return <StyleRoot><Homepage /></StyleRoot>
}

export default Felocity;
